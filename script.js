document.addEventListener('DOMContentLoaded', () => {    const topicInput = document.getElementById('topicInput');    const digestBtn = document.getElementById('digestBtn');    const summaryOutput = document.getElementById('summaryOutput');    // This is a placeholder for actual API integration.    // In a real application, you would send topicInput.value to a backend    // which would then call the Gemini API and return a summary.    digestBtn.addEventListener('click', () => {        const topic = topicInput.value.trim();        if (topic) {            summaryOutput.style.display = 'block';            summaryOutput.innerHTML = `<p>Searching for information on "<strong>${topic}</strong>"...</p><p>Generating summary with Gemini AI...</p>`;            // Simulate an API call            setTimeout(() => {                const dummySummary = `<h3>Summary for "${topic}"</h3><p>This is a simulated summary for the topic you entered. In a real application, the Gemini API would provide a concise and easy-to-understand digest of relevant information found on the web about "<strong>${topic}</strong>". This would help you quickly grasp the essence of complex subjects without reading multiple articles.</p><p>For example, if the topic was 'Artificial Intelligence', the summary might cover its history, key concepts, applications, and future outlook.</p>`;                summaryOutput.innerHTML = dummySummary;            }, 2000); // Simulate 2-second delay for API response        } else {            summaryOutput.style.display = 'block';            summaryOutput.innerHTML = '<p style="color: red;">Please enter a topic to digest!</p>';        }    });});